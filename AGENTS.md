# AGENTS.md — WorldConquest 엔지니어링 프로세스 정본 (source of truth)

> 이 파일은 **repo 메커니즘(빌드·커밋·검증·협업)의 정본**이다.
> **게임 설계·아키텍처·데이터 스키마·Phase 규율의 정본은 [`docs/GAME_DESIGN_SPEC.md`](docs/GAME_DESIGN_SPEC.md) (§0.3 필독)** 이며, 여기서는 재서술하지 않고 링크만 한다.
> **충돌 시 우선순위**: 설계·밸런스 내용 → 스펙이 이긴다. 엔지니어링 절차 → 이 파일이 이긴다.

## 0. 현재 상태 (확인일 병기 — stale 금지)

- **현재 Phase**: Phase 1 완료 + Phase 2 진행 중(전투·외교·AI·초빙·시네마틱 T0·내정 §2.3.1+§2.3.2 수치제 경제/파견 모델·UE5 클라이언트·API 계층, xUnit 226). (2026-07-17 확인)
- **표현층**: 콘솔(오라클) + UE5 클라이언트(`ue/WorldConquestUE`, 지도·도시·전투 화면) — Core↔UE5 는 API 계층(ApiServer/GameSessionHost, ue5-client-design 참조)으로 연결. (2026-07-17 확인)
- 스펙 v1.4 반영 증분(§2.7 시네마틱·§2.8 초빙·§5.6 밸런스 패널·§4.4 정수 산술·id 생애주기)은 **설계 확정, 구현 대기** 상태.
- 이 브랜치는 **orphan 브랜치**(`master` = 별개 프로젝트 YUNU_GAME 과 공통 조상 없음). **통합 경로 미정** — 별도 repo 분리 vs 영구 브랜치 유지를 사용자와 확정할 것. 그 전까지 커밋은 pathspec 으로 이 프로젝트 경로에 한정한다.

## 1. 검증 — `verify`

커밋 전 필수 (스펙 §0.3-6):

```
dotnet build -warnaserror
dotnet test
```

- 깨진 상태로 커밋하지 않는다. 깨지면 직전 녹색 커밋으로 되돌린다.
- CI: `.github/workflows/verify.yml` 가 push 마다 기계 강제 (이 브랜치·WorldConquest 경로 한정 — `master` 에서 돌지 않음).
- 무거운 검사(50턴 스모크·배치 시뮬·리플레이 골든)가 생기면 같은 체인에 이어 붙인다. 빈 2티어를 선제 생성하지 않는다.

## 2. 커밋 규율 (스펙 §0.3-6·7)

1. **녹색이면 자주 커밋** — 큰 변경을 미커밋으로 두지 않는다.
2. **pathspec 한정** — `git add <경로>` 로 대상을 좁힌다. 같은 repo 의 다른 프로젝트(master 계열) 파일을 섞지 않는다.
3. **스키마 변경 원자성** — 세이브·데이터 스키마 형태 변경 = `SaveVersion`/`schema_version` 상향 + 마이그레이션·호환 판정 + roundtrip 테스트를 **같은 커밋**에.
4. **move ≠ improve** — 동작 보존 '이동'과 동작 변경 '개선'을 한 커밋에 섞지 않는다 (Phase 3 콘솔→UE5 포팅의 핵심 규율).

## 3. 작업 이력 — 실패까지 기록한다

- 의미 있는 실패·되돌림·기각한 규칙/밸런스 안·하지 않기로 한 접근은 **커밋/최종 보고 전** [`docs/work-history.md`](docs/work-history.md) 에 한 줄 기록 [SHOULD].
- 같은 문제가 다시 보이면 이 문서를 먼저 확인한다.
- 사고가 반복되는 시스템은 정본 히스토리 문서를 둔다 (첫 사고 또는 스키마 버전 도입 시점 — 예: Phase 1 `docs/save-system-history.md`). 헤더 계약 = "건드리기 전 §아키텍처·§함정 필독, 변경 후 §이력 한 줄 추가".

## 4. 로컬 전용 파일 정책 (gitignore 산출물)

`balance_overrides.json`·`telemetry/*.jsonl`·자동저장 링 등 추적하지 않는 산출물도 스키마가 진화한다:

- 로컬 전용 파일에도 **버전 필드**(오버라이드=`schema_version`, JSONL=행별 버전)를 둔다.
- 실패 정책 3분법: **정의 데이터 = fail-fast**(§5.5 기동 실패) / **세이브 = fail-soft + 고지** / **로컬 실험·기록 = fail-soft + 반드시 고지**(무음 금지 — 미인식·버전 불일치 시 '몇 건 무시됨' 출력).

## 5. 멀티에이전트 협업 (Codex + Claude Code 병행 대비)

1. 공유 파일(지휘자 클래스·`main`·큰 JSON)은 **한 번에 한 에이전트만**.
2. 자기 작업은 리프 모듈에서 먼저 끝내고, 공유 파일 배선은 마지막에 한 번에.
3. 병행 시 **§5 데이터 우선 경로**를 기본 선택(기능을 데이터로 표현해 공유 코드 변경 0).
4. 편집 직후 검증: C# 코드 → `dotnet build` / `data/*.json` → `dotnet test`(§5.5 DataLoader 검증 경유 — build 만으로는 JSON 오매칭이 통과).
5. `Edit` 이 'file modified since read' 로 실패하면 상대 작업 중 신호 — 재시도 race 금지, 정지 후 확인.

## 6. 툴체인·의존성 수명

- SDK 핀: `global.json` (.NET 8).
- **⚠ .NET 8 LTS EOL = 2026-11-10** (확인일 2026-07-16). Phase 2~5 진행 중 도래 — **.NET 10(차기 LTS) 승급을 Phase 2 게이트 리뷰 안건**에 포함. 핀 갱신은 `verify` 녹색 + 골든 리플레이 무변화를 통과한 **단독 커밋**으로만.

## 7. 비공개·IP (스펙 §1·§9)

- **repo 퍼블릭 전환·배포·외부 공유 절대 금지** (타사 IP 크로스오버 캐릭터).
- IP 참조 이미지의 **클라우드 AI 서비스 업로드 금지** [MUST] — 로컬 생성만.
