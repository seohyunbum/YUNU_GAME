# AI에게 부탁할 때 쓰는 프롬프트

> 코드 변경을 부탁하기 전에, AI 에게 **`AGENTS.md` 를 먼저 읽으라**고 하세요. 거기에 어디에 코드를 넣고 어떻게 안전하게 바꾸는지가 적혀 있습니다.

## 처음 아이디어를 게임 규칙으로 바꾸기

```text
초등학생 아이가 생각한 게임 아이디어를 Vite + TypeScript + Three.js 프로젝트에 맞는 작은 구현 계획으로 바꿔줘.
한 번에 다 만들지 말고, 오늘 30분 안에 만들 수 있는 첫 번째 버전을 제안해줘.
아이가 Minecraft와 Roblox를 좋아하므로, 블록 만들기, 아바타 이동, 장애물 코스, 아이템 수집, 월드 규칙 바꾸기 중 어떤 재미를 빌려올지 같이 골라줘.

아이디어:
- 주인공:
- 좋아하는 게임에서 빌려오고 싶은 재미:
- 목표:
- 재미있는 규칙:
- 배경:
- 조작:
```

## 현재 게임에 기능 추가·변경하기

```text
먼저 AGENTS.md 를 읽고 이 프로젝트의 모듈 구조와 규칙을 따라줘.

C:\ai-game-lab 게임에 아래 아이디어를 반영해줘.
- 신규 로직은 src/main.ts 가 아니라 적절한 src/game/ 모듈에, 화면 표현은 src/ui/ 에 넣어줘.
- main.ts 는 새 모듈을 연결(배선)하는 최소 변경만 해줘.
- 기존 게임이 계속 실행되게 유지하고, 변경 범위는 작게 해줘.
- 끝나면 npm run verify 가 통과하는지 확인해줘.
- 실패하거나 되돌린 시도는 docs/work-history.md 에 결과와 이유까지 기록해줘.

바꾸고 싶은 점:
-
```

## main.ts 를 더 작게 쪼개기 (리팩터링)

```text
AGENTS.md 의 "추출 7단계 의식"을 따라줘.
src/main.ts 에서 아래 기능을 골라 src/game/ 또는 src/ui/ 모듈로 "동작을 바꾸지 말고 이동"만 해줘.
this.X 의존은 좁은 컨텍스트/인자로 주입하고, main.ts 에는 위임만 남겨줘.
끝나면 npm run verify 가 녹색인지 확인하고, 동작 변경(개선)은 다음 단계로 미뤄줘.
실패하거나 보류한 접근은 docs/work-history.md 에 결과와 이유까지 기록해줘.

쪼개고 싶은 기능:
-
```

## 오류 해결 부탁하기

```text
npm run dev 또는 npm run build 또는 npm run verify 에서 오류가 났어.
아래 오류 메시지를 보고 원인을 설명한 뒤, 필요한 파일을 수정해서 고쳐줘.

오류 메시지:
```

## 아이와 같이 회고하기

```text
오늘 만든 게임을 초등학생도 이해할 수 있는 말로 짧게 정리해줘.
잘된 점 3개, 다음에 해볼 아이디어 3개를 제안해줘.
실패했거나 되돌린 시도가 있으면 왜 그랬는지도 짧게 적어줘.
```
